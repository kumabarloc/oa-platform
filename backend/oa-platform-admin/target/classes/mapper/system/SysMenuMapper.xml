<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.oa.system.mapper.SysMenuMapper">

    <resultMap id="SysMenuResult" type="com.oa.system.entity.SysMenu">
        <id property="id" column="id"/>
        <result property="menuName" column="menu_name"/>
        <result property="parentId" column="parent_id"/>
        <result property="orderNum" column="order_num"/>
        <result property="path" column="path"/>
        <result property="component" column="component"/>
        <result property="query" column="query"/>
        <result property="isFrame" column="is_frame"/>
        <result property="isCache" column="is_cache"/>
        <result property="menuType" column="menu_type"/>
        <result property="visible" column="visible"/>
        <result property="status" column="status"/>
        <result property="perms" column="perms"/>
        <result property="icon" column="icon"/>
    </resultMap>

    <select id="selectMenusByUserId" resultMap="SysMenuResult">
        SELECT DISTINCT m.*
        FROM sys_menu m
        INNER JOIN sys_role_menu rm ON m.id = rm.menu_id
        INNER JOIN sys_user_role ur ON rm.role_id = ur.role_id
        WHERE ur.user_id = #{userId}
          AND m.del_flag = 0
          AND m.status = 0
        ORDER BY m.parent_id, m.order_num
    </select>

    <select id="selectMenusByRoleId" resultMap="SysMenuResult">
        SELECT m.*
        FROM sys_menu m
        INNER JOIN sys_role_menu rm ON m.id = rm.menu_id
        WHERE rm.role_id = #{roleId}
          AND m.del_flag = 0
        ORDER BY m.parent_id, m.order_num
    </select>

    <select id="selectMenuTree" resultMap="SysMenuResult">
        SELECT * FROM sys_menu WHERE del_flag = 0 ORDER BY parent_id, order_num
    </select>

    <select id="selectMenuById" resultMap="SysMenuResult">
        SELECT * FROM sys_menu WHERE id = #{id} AND del_flag = 0
    </select>

</mapper>