<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.oa.system.mapper.SysUserMapper">

    <resultMap id="SysUserResult" type="com.oa.system.entity.SysUser">
        <id property="id" column="id"/>
        <result property="deptId" column="dept_id"/>
        <result property="postId" column="post_id"/>
        <result property="empNo" column="emp_no"/>
        <result property="userName" column="user_name"/>
        <result property="nickName" column="nick_name"/>
        <result property="userType" column="user_type"/>
        <result property="email" column="email"/>
        <result property="phone" column="phone"/>
        <result property="sex" column="sex"/>
        <result property="avatar" column="avatar"/>
        <result property="password" column="password"/>
        <result property="status" column="status"/>
        <result property="loginIp" column="login_ip"/>
        <result property="loginDate" column="login_date"/>
    </resultMap>

    <select id="selectByUserName" resultMap="SysUserResult">
        SELECT * FROM sys_user WHERE user_name = #{userName} AND del_flag = 0
    </select>

    <select id="selectByEmpNo" resultMap="SysUserResult">
        SELECT * FROM sys_user WHERE emp_no = #{empNo} AND del_flag = 0
    </select>

    <select id="selectUserList" resultType="com.oa.system.domain.vo.UserListVo">
        SELECT
            u.id,
            u.user_name AS userName,
            u.nick_name AS nickName,
            u.emp_no AS empNo,
            u.dept_id AS deptId,
            u.post_id AS postId,
            u.phone,
            u.email,
            u.avatar,
            u.status,
            d.dept_name AS deptName,
            p.post_name AS postName
        FROM sys_user u
        LEFT JOIN sys_dept d ON u.dept_id = d.id
        LEFT JOIN sys_post p ON u.post_id = p.id
        WHERE u.del_flag = 0
        <if test="userName != null and userName != ''">
            AND u.user_name LIKE CONCAT('%', #{userName}, '%')
        </if>
        <if test="phone != null and phone != ''">
            AND u.phone LIKE CONCAT('%', #{phone}, '%')
        </if>
        <if test="status != null">
            AND u.status = #{status}
        </if>
        ORDER BY u.id DESC
    </select>

</mapper>
